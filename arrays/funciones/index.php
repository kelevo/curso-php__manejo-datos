<?php

// $courses = [
//     10 => 'php',
//     100 => 'javascript',
//     1000 => 'laravel'
// ];

$courses = ['php', 'javascript', 'laravel'];

//sort($courses);
// De manera descendente: rsort($courses);
// Ordenar usando $key: ksort($courses);
// Ordenar usando $key descendente: krsort($courses);

echo "<pre>";
//var_dump($courses);

// Rebanada (quitamos el elemento 1)
//var_dump(array_slice($courses, 1));

// Pedazo (divide el array en mas arrays (numero que le pasemos))
//var_dump(array_chunk($courses, 2));

// array_shift(); elimina el primer elemento y lo retorna
//array_shift($courses);

// array_pop(); elimina el ultimo elemento y lo retorna
//array_pop($courses);

// array_unshift(); agrega elementos en el inicio del aray
//array_unshift();

// array_push(); agrega un elemento al final
//array_push();

// array_flip(); intercambia keys por valores
